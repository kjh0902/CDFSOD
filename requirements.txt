# Python dependencies that are safe to install before building MMCV.
#
# Do not put torch, torchvision, mmcv, or -e ./mmdetection here.
# Install order:
#   1. torch/torchvision CUDA wheel
#   2. this file
#   3. mmcv source build against the installed torch
#   4. ./mmdetection editable install with --no-build-isolation

setuptools==80.9.0
wheel==0.45.1
mmengine==0.10.7

numpy==1.26.4
opencv-python==4.11.0.86
pycocotools==2.0.8
scipy==1.13.1
shapely==2.0.7
matplotlib==3.9.4
pillow==11.2.1
terminaltables==3.1.10
tqdm==4.67.1
PyYAML==6.0.2
packaging==25.0

transformers>=4.57.0,<5.0.0
tokenizers>=0.22.0,<0.24.0
huggingface-hub>=0.34.0,<1.0
safetensors==0.5.3
regex==2024.11.6
requests==2.32.3
ftfy==6.3.1
nltk==3.9.1

yapf==0.40.2
