# Runtime dependencies for the vendored MMDetection 3.3.0 code.
#
# PyTorch and MMCV are intentionally installed by scripts/install_rtx5090.sh:
# - torch/torchvision must come from the official CUDA 12.8 wheel index.
# - MMCV must be compiled for the active torch ABI and RTX 5090 (sm_120).

addict==2.4.0
matplotlib==3.10.8
nltk==3.9.2
numpy==1.26.4
opencv-python-headless==4.11.0.86
packaging==25.0
pillow==11.3.0
pycocotools==2.0.10
pyyaml==6.0.2
rich==14.2.0
scipy==1.15.3
sentencepiece==0.2.0
shapely==2.1.2
six==1.17.0
termcolor==3.2.0
terminaltables==3.1.10
transformers==4.57.1
yapf==0.43.0

# MMEngine 0.10.7 is installed with --no-deps by install_rtx5090.sh after
# this file so that its opencv-python dependency does not replace the
# headless server build above.
