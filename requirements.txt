# Python/runtime dependencies for the GroundingDINO baseline.
#
# Install order matters for this project:
#   1. Install torch/torchvision with the CUDA wheel that matches the GPU.
#   2. Install this file.
#   3. Install MMCV with the matching OpenMMLab/CUDA/PyTorch build path.
#   4. Install ./mmdetection in editable mode with --no-build-isolation.
#
# Do not put torch, torchvision, mmcv, or -e ./mmdetection here. MMCV contains
# C++/CUDA ops and must be installed after torch is already importable.

openmim==0.3.9
mmengine==0.10.7

numpy==1.26.4
opencv-python==4.11.0.86
pycocotools==2.0.8
scipy==1.13.1
shapely==2.0.7
matplotlib==3.9.4
pillow==11.2.1
terminaltables==3.1.10
tqdm==4.67.1
PyYAML==6.0.2
packaging==25.0

transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.30.2
safetensors==0.5.3
regex==2024.11.6
requests==2.32.3
ftfy==6.3.1
nltk==3.9.1

yapf==0.40.2
